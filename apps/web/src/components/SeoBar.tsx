export function SeoBar() {
  return (
    <div className="w-full border-b border-[var(--border)] bg-[#f5f5f5]">
      <div className="mx-auto max-w-5xl px-4 py-3">
        <p className="text-center text-sm leading-relaxed text-[var(--muted)]">
          Check DNS, RDAP, TLS, certificate chains, ASN, and socket banners in one probe — with
          DNSSEC validation, Certificate Transparency logs, and expiry warnings.
        </p>
      </div>
    </div>
  );
}
