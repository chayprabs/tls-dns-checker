name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: read
  packages: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install
      - run: pnpm build

      - name: Build worker image
        run: docker build -f apps/worker/Dockerfile -t tls-dns-checker-worker:${{ github.ref_name }} .

      - name: Build web image
        run: docker build -f apps/web/Dockerfile -t tls-dns-checker-web:${{ github.ref_name }} .
